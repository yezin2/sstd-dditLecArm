from flask import Flask,request

app = Flask(__name__)

@app.route('/')
def home():
    a = request.args.get('a','haha')
    return 'Hello, {}'.format(a)

@app.route('/<a>')
def aParam(a):
    return f'{a}!'
if __name__ == '__main__':
    app.run(debug=True)