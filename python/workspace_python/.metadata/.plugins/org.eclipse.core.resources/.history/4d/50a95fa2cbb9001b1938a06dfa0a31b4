import cv2

img = cv2.imread('gong.png')
print(len(img))
print(len(img[0]))
crop= color[50:200,250:500]
cv2.imwrite('face.png', crop)

print(img)

cv2.imshow('testimage', crop)
cv2.waitKey(0)  # 키 입력할 때까지 무한 대기, 0 이면 무한대기
cv2.destroyAllWindows()