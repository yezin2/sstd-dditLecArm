<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
         PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
         "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="board">

<!--
      이 영역에 sql에  맞는 태그를 사용하여 SQL문을 기술한다.
      
      사용할 수 있는 기본적인 태그들
      <select> ~ </select>
      <insert> ~ </insert>
      <update> ~ </update>
      <delete> ~ </delete>
      
      위 태그에서 사용되는 속성들
      
      1) id속성 : 해당 태그를 호출할 때 <sqlMap>태그의 namespace와 연결하여 사용하는 이름

      2) parameterClass속성 : sql문에 사용될 데이터가 들어있는 객체를 지정한다.
         (보통 VO클래스, 자바의 자료형이름 등이 사용된다.)
         (typeAlias로 지정한 alias명을 사용할 수 있다.)

      3) resultClass 속성 : select문을 실행한 결과를 담을 객체를 지정한다.
                     (보통 VO클래스나 자바의 자료형 이름을 사용한다.)
  -->

	<insert id="insertBoard" parameterClass="kr.or.ddit.board.vo.BoardVO">
		insert into board
		(board_no, board_tit, board_aut, board_cont, board_day)
		values(board_no_seq.nextval,#boardTit#,#boardAut#,#boardCont#,sysdate)
	</insert>
	<update id="updateBoard" parameterClass="kr.or.ddit.board.vo.BoardVO">
		update board set board_tit = #boardTit# ,board_aut = #boardAut# , 
							board_cont = #boardCont# where board_no = #boardNo#
	</update>
	<delete id="deleteBoard" parameterClass="String">
		delete from board where board_no = #boardNo#
	</delete>
	
	<resultMap class="kr.or.ddit.board.vo.BoardVO" id="boardMap">
		<result property="boardNo" column="board_no"/>
		<result property="boardTit" column="board_tit"/>
		<result property="boardAut" column="board_aut"/>
		<result property="boardCont" column="board_cont"/>
		<result property="boardDay" column="board_day"/>
	</resultMap>
	<select id="getBoardAll" resultMap="boardMap">
		select * from board
	</select>
	<select id="getBoard" parameterClass="String" resultClass="int">
		select count(*) as cnt 
		from board where board_no = #boardNo#
	</select>
	<select id="getSearchBoard" parameterClass="BoardVO" resultMap="boardMap">
		select * from board where 1=1 
		<isNotEmpty property="boardNo">
			and board_no = #boardNo# 
		</isNotEmpty>
		<isNotEmpty property="boardTit">
			and board_tit like '%' || #boardTit# || '%'
		</isNotEmpty>
		<isNotEmpty property="boardAut">
			and board_aut = #boardAut# 
		</isNotEmpty>
		<isNotEmpty property="boardCont">
			and board_cont like '%' || #boardCont# || '%'
		</isNotEmpty>
							
	</select>
	
</sqlMap>         




